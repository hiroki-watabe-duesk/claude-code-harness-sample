# CLAUDE.md

このプロジェクトは、3つのサブエージェント（**プランナー → ジェネレーター → エバリュエーター**）が連携する、スプリント駆動の自動開発パイプラインです。このファイルは、3つのエージェントが矛盾なく協調動作するための共通ルール（契約）を定義します。**すべてのエージェントは作業開始前にこのファイルに従ってください。**

---

## パイプライン全体像

```
  ┌─────────────┐    spec.md    ┌───────────────┐   sprint-N-self-eval.md   ┌─────────────────┐
  │  プランナー  │ ───────────▶ │ ジェネレーター │  ───────────────────────▶ │  エバリュエーター│
  │  (planner)  │               │  (generator)  │                           │  (evaluator)    │
  └─────────────┘               └───────────────┘                           └─────────────────┘
                                       ▲                                          │
                                       │        sprint-N-feedback.md (FAIL時)     │
                                       └──────────────────────────────────────────┘
                                                  修正ループ

  PASS → 次のスプリントへ                          FAIL → ジェネレーターが修正して再評価
```

3エージェントは**直接会話しません**。すべての連携は下記「ファイル規約」のファイルを介して行われます。ファイルがエージェント間の唯一の通信手段です。

---

## 黄金ルール（最優先の原則）

1. **責任境界を越えない** — プランナーは「何を」、ジェネレーターは「どう」、エバリュエーターは「検証」。互いの領域に踏み込まない。
2. **1回の呼び出し＝1スプリント** — ジェネレーターは一度に複数スプリントを実装しない。エバリュエーターは一度に1スプリントだけ評価する。
3. **エバリュエーターを飛ばさない** — ジェネレーターの自己評価がどれだけ高くても、必ずエバリュエーターの合格判定を経てから次スプリントへ進む。
4. **`spec.md` が唯一の真実** — 仕様の根拠は常に `spec.md`。記憶や推測ではなく、必ずファイルを読む。

---

## 各エージェントの責任境界

| エージェント | やること | やってはいけないこと |
|---|---|---|
| **プランナー** | アイデアを `spec.md` に展開。機能・スプリント・受け入れ基準を定義 | 技術選定、コード記述、実装方法の指定（DB構成・フレームワーク等） |
| **ジェネレーター** | 1スプリントを実装。技術選定。自己評価を記録 | 複数スプリントの同時実装、`spec.md` の書き換え、エバリュエーターの仕事の代行 |
| **エバリュエーター** | Playwrightで実動作を検証。合否判定とフィードバック作成 | コードの修正、自己評価の鵜呑み、未再現バグの報告 |

---

## ファイル規約（エージェント間の契約）

| ファイル | 書く人 | 読む人 | 役割 |
|---|---|---|---|
| `spec.md` | プランナー | 全員 | 製品仕様の唯一の真実。機能・スプリント・受け入れ基準 |
| `sprint-N-self-eval.md` | ジェネレーター | エバリュエーター | 実装後の自己評価。引き渡し合図 |
| `sprint-N-feedback.md` | エバリュエーター | ジェネレーター | 評価結果。FAIL時の必須修正事項 |
| `PROGRESS.md` | ジェネレーター / エバリュエーター | 全員 | スプリントの進捗・技術スタック・現在地 |

- `N` は必ずスプリント番号に置換する（例: `sprint-3-feedback.md`）。
- これらのファイル名は厳守する。命名がずれると後続エージェントがファイルを見つけられず連携が破綻する。

---

## ワークフローの進め方（オーケストレーション）

メインセッション（指揮役）は以下の順序でサブエージェントを呼び出す。

### フェーズ1: 計画
1. ユーザーのアイデア（1〜4行）を**プランナー**に渡す。
2. プランナーが `spec.md` を生成する。
3. ユーザーに `spec.md` の概要を提示し、着手の合図を待つ。

### フェーズ2: 実装と評価のループ（スプリントごとに繰り返す）
1. **ジェネレーター**に「スプリントNを実装して」と指示する。
2. ジェネレーターが実装し、`sprint-N-self-eval.md` を出力する。
3. **エバリュエーター**に「スプリントNを評価して」と指示する。
4. エバリュエーターが `sprint-N-feedback.md` を出力し、判定を返す。
   - **PASS** → `PROGRESS.md` を更新し、スプリント N+1 へ進む。
   - **FAIL** → 同じ**ジェネレーター**に「スプリントNのフィードバックを修正して」と指示する。`sprint-N-feedback.md` の必須修正事項をすべて対応させ、手順3に戻る。
5. 全スプリントが PASS になったら完了。

### 修正ループの上限
- 同一スプリントで **3回連続 FAIL** したら、ループを止めてユーザーに状況を報告する（無限ループ防止）。
- このとき、繰り返し失敗している基準と、考えられる原因を要約して提示する。

---

## 進捗管理（`PROGRESS.md`）

スプリントをまたいで状態を共有するため、`PROGRESS.md` を維持する。最初のスプリント着手時にジェネレーターが作成し、以後ジェネレーターとエバリュエーターが更新する。

```markdown
# 進捗

## 技術スタック（スプリント1でジェネレーターが決定）
- 言語 / ランタイム:
- 主要フレームワーク・ライブラリ:
- データストア:
- 起動コマンド:
- アプリのURL:

## スプリント状況
| スプリント | 状態 | 試行回数 | 最終更新 |
|---|---|---|---|
| 1 | DONE / IN_PROGRESS / BLOCKED / TODO | 1 | YYYY-MM-DD |
```

- **技術スタックはスプリント1で確定し、以降のスプリントで一貫させる。** 後続のジェネレーターは必ずこの記録を読み、勝手に技術を切り替えない。
- **起動コマンドとURLを必ず記録する。** エバリュエーターはこれを使ってアプリを起動・操作する。

---

## やってはいけないこと

- ❌ プランナーが技術スタックや実装方法を `spec.md` に書く（下流に誤りが伝播する）
- ❌ ジェネレーターが受け入れ基準を満たさないスタブ・TODO を残してエバリュエーターに渡す
- ❌ ジェネレーターが一度の呼び出しで複数スプリントを実装する
- ❌ エバリュエーターが自分でコードを修正する（検証専任。修正はジェネレーターの仕事）
- ❌ エバリュエーターの判定を経ずに次スプリントへ進む
- ❌ FAIL のフィードバックを一部だけ対応して再評価に出す（必須修正事項は全件対応）
- ❌ `spec.md` をジェネレーターやエバリュエーターが書き換える（仕様変更が必要ならユーザーに確認し、プランナーに戻す）

---

## 環境メモ

- Playwright MCP サーバーは `.mcp.json` に設定済み（エバリュエーターが使用）。
- 各スプリント終了時、アプリは常に実行可能な状態を保つこと（エバリュエーターが起動して操作するため）。
